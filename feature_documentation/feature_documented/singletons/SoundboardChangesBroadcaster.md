# Do
- connect to dropped file catcher to listen for incoming files
- generate soundboard button data upon receiving files
- call text input dialog for each new button to collect button names
- signal the soundboard button data once all data is generated and collected

# Do not
- spawn button to the soundboard.

# Functions
- start_playing_preview(file_path) - plays a preview with an interval of 1 second by default
- stop_playing_preview()
