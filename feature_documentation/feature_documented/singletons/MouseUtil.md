MouseUtil is listed as singleton because it needs to be inside a tree to function.

# Do
- calculate mouse delta position from click to current
- deal with mouse-related functions

# Functions
- get_delta_from_pos(click_position:Vector2) -> Vector2 - returns global delta from the position.
