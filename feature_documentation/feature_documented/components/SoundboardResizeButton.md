# Do
- have reference to content switcher
- set size to be bigger every zoom in click by 25 pixels
- set size to be smaller every zoom out click by 25 pixels

# Functions
- get_soundboard()
- set_increment()
- set_size_indicator()
