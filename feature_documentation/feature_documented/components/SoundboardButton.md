# Do's
- displays sound name
- displays duration
- displays icon
- displays progress bar indicating it's playing sound
- can be clicked to play sound
- can be clicked again to stop playing sound
- displays the "edit" button
- contain the audiostream node
- have its own volume slider
- have a function that allows it to resize
- have a reposition button, when clicked will enter the "moving" mode.
- allows moving mode if parent is "Soundboard" class.
- moving mode is using "move_child()" function

# Functions
- get_button_data() - search by looping and matching ID
- get_button_name() - search by looping and matching ID
- get_button_volume() - search by looping and matching ID
- get_button_tags() - search by looping and matching ID
- is_playing() - search by looping and matching ID
- set_button_data()
- set_button_name()
- set_audio()
- set_volume
- set_index()
- set_tags()
- set_all_icon_invisible() -> void - the icons in the icon panel
- switch_icon_visible() -> void - it only takes texture rect argument
- start_editing()
- cancel_editing()
- confirm_editing()
- close_editor()
- delete_sound_button()
