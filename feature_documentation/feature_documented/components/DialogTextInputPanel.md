# Do
- have 2 buttons that represents cancel and confirm
- have a line edit
- sends a string and a boolean as an array as a response signal

# Don't
- delete itself after any button is pressed
- do anything else 

# functions
- setup_dialog(message:String) - sets up button to have a message and un-disable the confirm and cancel button.
- set_dialog_message(mesage:String) - changes the button's dialog message to be the
