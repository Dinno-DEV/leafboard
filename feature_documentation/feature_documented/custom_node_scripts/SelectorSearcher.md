# Do
- reference selector node
- filter the button_id's in selector node

# Functions
- get_all_id_buttons
- set_all_id_buttons_invisible - also turns selector node invisible
- set_all_id_buttons_visible - also turns selector node visible
- set_id_button_visible - also turns selector node visible
